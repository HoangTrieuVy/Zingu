#pragma once

// Copy this file to secrets.h and fill in your own network.
// secrets.h is gitignored — do not commit it.

constexpr const char* ssid = "YOUR_WIFI_SSID";
constexpr const char* pass = "YOUR_WIFI_PASSWORD";
